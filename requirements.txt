# ---- analysis (notebooks 01-04) ----
pandas>=2.0
numpy>=1.24
jupyter>=1.0
matplotlib>=3.7
tabulate>=0.9          # required by DataFrame.to_markdown()

# ---- dashboard (streamlit_app.py) ----
streamlit>=1.40
plotly>=5.24,<6        # pinned: plotly 6 switched maps to MapLibre and the map
                       # renders as a grey box against Streamlit's bundled plotly.js
requests>=2.31

# ---- mapping (optional, for future spatial work) ----
# geopandas>=0.14
# shapely>=2.0
